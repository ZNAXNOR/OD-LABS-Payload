import { MediaGridBlock as MediaGridBlockProps } from 'src/payload-types'

import React from 'react'

export const MediaGridBlock: React.FC<MediaGridBlockProps> = (props) => {
  return (
    <div className="container">
      <p>MediaGrid Block</p>
    </div>
  )
}
