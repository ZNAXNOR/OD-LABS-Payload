import { MediaCollageBlock as MediaCollageBlockProps } from 'src/payload-types'

import React from 'react'

export const MediaCollageBlock: React.FC<MediaCollageBlockProps> = (props) => {
  return (
    <div className="container">
      <p>MediaCollage Block</p>
    </div>
  )
}
