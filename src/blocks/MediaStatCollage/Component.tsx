import { MediaStatCollageBlock as MediaStatCollageBlockProps } from 'src/payload-types'

import React from 'react'

export const MediaStatCollageBlock: React.FC<MediaStatCollageBlockProps> = (props) => {
  return (
    <div className="container">
      <p>MediaStatCollage Block</p>
    </div>
  )
}
